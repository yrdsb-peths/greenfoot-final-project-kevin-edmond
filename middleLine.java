import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class middleLine here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class middleLine extends Actor
{
    public middleLine()
    {
        getImage().setColor(Color.WHITE);
        getImage().fillRect(0,0,600,1);
    }
    /**
     * Act - do whatever the middleLine wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        
    }
}
